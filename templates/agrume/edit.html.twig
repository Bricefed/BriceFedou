{% extends 'base.html.twig' %}

{% block title %}Edit Agrume{% endblock %}

{% block body %}
    <h1 class="mt-5 mb-4">Modifier l'agrume</h1>

    {{ include('agrume/_form.html.twig', {'button_label': 'Valider'}) }}

    <a href="{{ path('agrume_index') }}" class="btn btn-info mt-2">Revenir en arrière</a>

    {{ include('agrume/_delete_form.html.twig') }}
{% endblock %}
